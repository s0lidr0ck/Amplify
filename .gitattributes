# Shell scripts and unit files run on Linux. Windows checkouts must not turn
# their line endings into CRLF: bash reads the trailing \r as part of the
# command and fails with messages that point nowhere near the real problem
# ("$'\r': command not found").
*.sh text eol=lf
*.service text eol=lf
*.py text eol=lf
